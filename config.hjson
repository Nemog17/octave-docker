{
  redis: {
    hostname: "localhost"
    port: 6379
  }

  mongo: { hostname: "" }   # desactivado

  front: {
    hostname: "0.0.0.0"
    port: 8080
    require_https: false
    cookie: { secret: "s3cret-cooki3-key" }
  }

  back: {
    hostname: "127.0.0.1"
    port: 5000          # puerto interno para front↔back (no expuesto)
  }

  session: {
    implementation: "unsafe"
    maxCpuTime: 60      # segundos por comando
  }
}
